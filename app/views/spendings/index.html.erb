 <nav class="main-blue-background d-flex justify-content-between align-items-end text-light login-page-nav p-3">
      <span>
        <%= link_to root_path, class: "bg-transparent text-light border-0", :method => :get do%>
        <span class="material-icons nav-search-icon ">
        west
        </span>
      <%end%>
      </span>

      <span>
      <h2>TRANSACTIONS</h2>
      </span>

      <span class="material-icons navbar-brand nav-search-icon">
        search
      </span>
     
    </nav>

<div id="spendings">
  <div class="groups-wrapper">

  <div class="d-flex justify-content-between align-items-center bg-light p-2 m-3"> 
    <%= image_tag @group.icon, :size => '100x100' %>
    <span><%= @group.name%></span>
    <div class="fw-bold">
      $<%=@spendings.reduce(0) { |sum, spending| sum + spending.amount }%>
    </div>
  </div>

  <% @spendings.each do |spending| %>
    <%= render spending %>
  <% end %>
  </div>
    <%= link_to "New Transaction", new_group_spending_path(params[:group_id]) , class:"new-group-button bg-success text-white text-decoration-none form-control p-4 text-center" %>

</div>

<%= link_to "Back", root_path %>

